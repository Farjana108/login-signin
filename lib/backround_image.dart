import 'package:flutter/material.dart';

class BackroundImage extends StatelessWidget {
  const BackroundImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
